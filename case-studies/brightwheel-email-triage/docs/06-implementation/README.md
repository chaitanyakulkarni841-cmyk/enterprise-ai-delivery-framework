# 06 — Make implementation walkthrough

[Case overview](../../README.md) · [Report index](../README.md)

**Awaiting upload.** Suggested filename: `make-implementation-guide.pdf`.

Include an annotated actual scenario screenshot, module-by-module explanation, fields/mappings, prompt and schema versions, routing configuration, connection requirements, test destinations, state, retries, and a normal/exception execution.

The PDF explains the build. Upload actual JSON exports and prompt/configuration files under the case's `solution/` and link them. Keep the GitHub prompt and deployed Make prompt synchronized through a documented release check.
